--INPUT
/foo#{bar}baz/
--LOCATIONS
~ begin ()
             ~ end ()
~~~~~~~~~~~~~~ expression ()
 ~~~ expression (src)
    ~~ begin (opts)
         ~ end (opts)
    ~~~~~~ expression (opts)
      ~~~ name (opts/stmt[0])
      ~~~ expression (opts/stmt[0])
--AST
s(:regexp,
  s(:str, "foo"),
  s(:begin,
    s(:lvar, "bar")),
  s(:str, "baz"),
  s(:regopt))
