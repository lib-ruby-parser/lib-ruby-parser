--INPUT
/source/im
--LOCATIONS
~ begin (root)
       ~ end (root)
~~~~~~~~~~ expression (root)
 ~~~~~~ expression (root -> parts -> 0)
        ~~ expression (root -> options)
--AST
s(:regexp,
  s(:str, "source"),
  s(:regopt, "i", "m"))
