--INPUT
foo.A += m foo
--LOCATIONS
    ~ selector ()
~~~~~~~~~~~~~~ expression ()
    ~ selector (recv)
~~~~~ expression (recv)
~~~ name (recv/recv)
~~~ expression (recv/recv)
--AST
s(:op_asgn,
  s(:send,
    s(:lvar, "foo"), "A"), "+",
  s(:send, nil, "m",
    s(:lvar, "foo")))
