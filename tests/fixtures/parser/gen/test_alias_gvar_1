--INPUT
alias $a $+
--LOCATIONS
~~~~~ keyword (root)
~~~~~~~~~~~ expression (root)
      ~~ expression (root -> to)
         ~~ expression (root -> from)
--AST
s(:alias,
  s(:gvar, "$a"),
  s(:back_ref, "$+"))
