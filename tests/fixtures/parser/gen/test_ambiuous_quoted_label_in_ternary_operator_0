--INPUT
a ? b & '': nil
--LOCATIONS
          ~ colon ()
  ~ question ()
~~~~~~~~~~~~~~~ expression ()
~ selector (cond)
~ expression (cond)
      ~ selector (truthy)
    ~~~~~~ expression (truthy)
    ~ selector (truthy/recv)
    ~ expression (truthy/recv)
        ~ begin (truthy/args[0])
         ~ end (truthy/args[0])
        ~~ expression (truthy/args[0])
            ~~~ expression (falsey)
--AST
s(:if,
  s(:send, nil, "a"),
  s(:send,
    s(:send, nil, "b"), "&",
    s(:str, "")),
  s(:nil))
