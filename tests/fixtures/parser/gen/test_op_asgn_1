--INPUT
foo::a += 1
--LOCATIONS
     ~ selector ()
~~~~~~~~~~~ expression ()
     ~ selector (recv)
~~~~~~ expression (recv)
~~~ name (recv/recv)
~~~ expression (recv/recv)
--AST
s(:op_asgn,
  s(:send,
    s(:lvar, "foo"), "a"), "+",
  s(:int, "1"))
