--INPUT
class Foo < a:b; end
--LOCATIONS
~~~~~ keyword ()
          ~ operator ()
      ~~~ name ()
                 ~~~ end ()
~~~~~~~~~~~~~~~~~~~~ expression ()
      ~~~ name (name)
      ~~~ expression (name)
            ~ selector (superclass)
            ~~~ expression (superclass)
             ~ begin (superclass/args[0])
             ~~ expression (superclass/args[0])
--AST
s(:class,
  s(:const, nil, "Foo"),
  s(:send, nil, "a",
    s(:sym, "b")), nil)
