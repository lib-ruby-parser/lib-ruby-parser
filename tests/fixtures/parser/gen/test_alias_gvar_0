--INPUT
alias $a $b
--LOCATIONS
~~~~~ keyword (root)
~~~~~~~~~~~ expression (root)
      ~~ expression (root -> to)
         ~~ expression (root -> from)
--AST
s(:alias,
  s(:gvar, "$a"),
  s(:gvar, "$b"))
